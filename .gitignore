# secrets — NEVER commit
service-account.json
*.gcp.json
.env
.env.*
google-credentials*.json

# python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.whl

# editor
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# hermes plan scratch (lives in macro-research, not qpr-autofill)
.hermes/plans/

# local cycle outputs (keep sources/ and notes/ but ignore generated)
*.tmp

# Note: qpr-autofill is tested using ~/Documents/Projects/macro-research/.venv
# (Python 3.11.15, already has gspread + pyyaml + bs4 + pytest). On a fresh
# machine, recreate with: python3.11 -m venv .venv && .venv/bin/pip install -e ".[dev]"
notes/
